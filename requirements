# Core simulation and RL stack.
# Versions are lower bounds that were actually used during development
# (Python 3.12 on Windows 11); newer minor releases should work.

# --- simulation ---
numpy>=1.26
PyYAML>=6.0

# --- reinforcement learning ---
gymnasium>=0.29
stable-baselines3>=2.3      # PPO implementation
torch>=2.2                  # backend for stable-baselines3
tensorboard>=2.15           # optional: training curves

# --- backend API ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- development / testing ---
pytest>=8.0
httpx>=0.27                 # required by fastapi.testclient
